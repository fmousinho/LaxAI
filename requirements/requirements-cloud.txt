# Cloud Training Job Dependencies
# Core Google Cloud dependencies for the training job orchestration system

# API runtime dependencies (also declared in pyproject.toml). Add here so
# builds that install from requirements/ get the API packages too.
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Firestore for job state management
google-cloud-firestore>=2.13.0

# Pub/Sub for job queueing 
google-cloud-pubsub>=2.18.0

# Cloud Functions framework
functions-framework>=3.5.0

# Cloud Storage for artifacts (optional)
google-cloud-storage>=2.10.0

# Cloud Logging
google-cloud-logging>=3.8.0

# General utilities
python-dateutil>=2.8.2
pydantic>=2.5.0
toml>=0.10.2

python-json-logger>=2.0.0
google-cloud-secret-manager==2.24.0


