#
# This file is autogenerated by pip-compile with Python 3.12
# by the following command:
#
#    pip-compile --output-file=requirements/requirements-cpu.lock.txt requirements/requirements-cpu.txt
#
# Ensure pip prefers CPU-only PyTorch wheels when building wheels in Docker.
-r requirements-base.txt

# All package pins are declared in `requirements/requirements-base.txt`.
# This file intentionally only references the base bundle so the CPU
# installation uses the CPU wheel index and torch pin from the base file.

# The following packages are considered to be unsafe in a requirements file:
# setuptools
