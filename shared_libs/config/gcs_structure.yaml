gcs:
  bucket_name: "laxai_dev"
  tenant_id: "{tenant_id}"
  data_prefixes:
    raw_data: "raw/"
    process_root: "process/"
    process_folder: "process/{video_id}/"
    detections_path: "process/{video_id}/detections.json"
    saved_graph: "process/{video_id}/stitcher_graph.gml"
    imported_video: "process/{video_id}/imported/"
    extracted_frames: "process/{video_id}/{frame_id}/"
    orig_crops: "process/{video_id}/{frame_id}/orig_crops/"
    augmented_crops_root: "process/{video_id}/{frame_id}/augmented_crops"
    augmented_crops: "process/{video_id}/{frame_id}/augmented_crops/{orig_crop_id}"
    datasets_root: "datasets"
    train_dataset: "datasets/{dataset_id}/train"
    val_dataset: "datasets/{dataset_id}/val"
    dataset_folder: "datasets/{dataset_id}"
    train_player_folder: "datasets/{dataset_id}/train/{orig_crop_id}"
    val_player_folder: "datasets/{dataset_id}/val/{orig_crop_id}"
    run_data: "runs/{run_id}/"
    unverified_tracks: "process/{video_id}/unverified_tracks/{track_id}/"
    track_graphs: "process/{video_id}/track_graphs.gml"
    track_graph_image: "process/{video_id}/track_graph.jpg"
    verified_tracks: "process/{video_id}/verified_tracks/{track_id}/"
    video_thumbnail: "process/{video_id}/thumbnail.jpg"