# Cloud Functions Requirements for Training Proxy
# Minimal dependencies for serverless function execution

# Google Cloud Platform services
google-cloud-pubsub>=2.31.1
google-cloud-run>=0.11.0

# Core dependencies (included in Cloud Functions runtime)
# google-api-core and protobuf are included in the Python 3.12 runtime