-r requirements-base.txt
opencv-python-headless
Pillow
supervision
rfdetr
# If you are NOT using a prebuilt pytorch CPU base image, add torch/torchvision CPU builds here:
# torch==<version>+cpu
# torchvision==<version>+cpu
# (Prefer installing torch from the official wheel index; see pytorch.org for pinned commands)
wandb
