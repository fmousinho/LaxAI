# Example .env for local development (do NOT commit real secrets)
# Copy to .env and populate real values before running the service locally.

# WandB API key (required for uploading runs)
WANDB_API_KEY=

# Hugging Face Hub token for gated model downloads (DINOv3)
HUGGINGFACE_HUB_TOKEN=

# Optional: Google Cloud project ID to enable Secret Manager fallback
GOOGLE_CLOUD_PROJECT=

# Optional: Path to a local service account key file (absolute path)
GOOGLE_APPLICATION_CREDENTIALS=

# Optional: Keep WandB offline during local dry-runs
#WANDB_MODE=offline
# Google Cloud Storage Configuration
# Path to your service account key file (JSON format)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# Optional: Override default project ID
GCS_PROJECT_ID=laxai

# Optional: Override default bucket name
GCS_BUCKET_NAME=laxai_dev

# Optional: Override default user path (folder structure in bucket)
GCS_USER_PATH=tenant1/user

# Example of actual paths (uncomment and modify as needed):
# GOOGLE_APPLICATION_CREDENTIALS=/Users/fernandomousinho/Documents/credentials/laxai-service-account.json
# GCS_PROJECT_ID=my-custom-project
# GCS_BUCKET_NAME=my-custom-bucket
# GCS_USER_PATH=production/user123
